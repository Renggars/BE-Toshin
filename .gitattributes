# Auto detect text files and perform LF normalization
* text=auto

# Shell scripts MUST use LF (not CRLF) to run in Linux containers
*.sh text eol=lf

# Docker files
Dockerfile text eol=lf
docker-compose.yml text eol=lf
.dockerignore text eol=lf
